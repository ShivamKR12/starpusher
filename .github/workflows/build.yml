name: Build Windows Executable

on:
  [push, workflow_dispatch]

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    if: github.event_name != 'push' || (!contains(github.event.head_commit.message, '[skip ci]') && !contains(github.event.head_commit.message, '[ci skip]'))

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pygame-ce pyinstaller

      - name: Build executable with PyInstaller
        run: |
          pyinstaller --clean --noconfirm startpusher.spec

      - name: Zip executable
        run: |
          Compress-Archive -Path dist\startpusher.exe -DestinationPath dist\startpusher.zip -Force

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.0.${{ github.run_number }}
          name: Star Pusher v1.0.${{ github.run_number }}
          files: dist/startpusher.zip
